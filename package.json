{
  "name": "my-vue-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "@types/three": "^0.157.0",
    "@vitejs/plugin-basic-ssl": "^1.2.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.1",
    "gts": "^6.0.2",
    "@types/node": "^22.7.5"
  },
  "dependencies": {
    "@zappar/zappar-threejs": "^3.0.1-beta.10",
    "three": "^0.157.0"
  }
}
